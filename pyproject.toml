[project]
name = "andruha-object-storage-service"
version = "0.1.0"
description = "Media upload and access boundary skeleton"
readme = "README.md"
requires-python = ">=3.14,<4.0"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
